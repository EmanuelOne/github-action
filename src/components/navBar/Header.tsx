import { Popover } from '@headlessui/react';

export default function Header() {
  return (
    <>
      <Popover className={'bg-white shadow-sm lg:static lg:overflow-y-visible'}>
        hey
      </Popover>
    </>
  );
}
